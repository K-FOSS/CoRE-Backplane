apiVersion: mylogin.space/v1alpha1
kind: User
metadata:
  name: {{ .Release.Name }}

spec:
  name: Authentik

  database:
    name: le6VpU1g8Q

  writeConnectionSecretToRef:
    name: {{ .Release.Name }}