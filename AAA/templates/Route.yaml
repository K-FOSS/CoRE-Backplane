apiVersion: gateway.networking.k8s.io/v1beta1
kind: HTTPRoute
metadata:
  name: authentik

  labels:
    wan-mode: 'public'
spec:
  parentRefs:
    - name: ingress-gw
  hostnames:
    - idp.mylogin.space
  rules:
    - backendRefs:
        - name: k0s-cntrl-aaa-authentik
          port: 80