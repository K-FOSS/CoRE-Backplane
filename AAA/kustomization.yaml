apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

patchesStrategicMerge:
  - |-
      apiVersion: apps/v1
      kind: Deployment
      metadata:
      name: k0s-cntrl-aaa-authentik-server

      labels:
          logs: loki-myloginspace

      spec:
      template:
          metadata:
          labels:
              logs: loki-myloginspace
