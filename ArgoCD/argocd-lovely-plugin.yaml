apiVersion: apps/v1
kind: Deployment
metadata:
  name: argocd-lovely-plugin
spec:
  template:
    spec:
      # -- init container to download vault plugin
      initContainers:
        - name: download-tools
          image: registry.access.redhat.com/ubi8
          env:
            - name: AVP_VERSION
              # renovate: datasource=github-releases depName=argoproj-labs/argocd-vault-plugin
              value: 1.17.0
          command: [sh, -c]
          args:
            - >-
              curl -L https://github.com/argoproj-labs/argocd-vault-plugin/releases/download/v$(AVP_VERSION)/argocd-vault-plugin_$(AVP_VERSION)_linux_amd64 -o argocd-vault-plugin &&
              chmod +x argocd-vault-plugin &&
              mv argocd-vault-plugin /custom-tools/
          volumeMounts:
            - mountPath: /custom-tools
              name: custom-tools

        - name: download-toolsgitpods
          image: kristianfjones/gitpods-testing:latest
          command: [sh, -c]
          args:
            - >-
              mv /installer /custom-tools/gitpods-installer
          volumeMounts:
            - mountPath: /custom-tools
              name: custom-tools
      containers:
        - name: lovely-plugin
          # This command is actually already set in the image.
          image: ghcr.io/crumbhole/lovely-vault-plugin:0.23.0
          # Here we are configuring default evironment for every app - in this case vault
          envFrom:
            - secretRef:
                name: argocd-vault-config
          securityContext:
            runAsNonRoot: true
            runAsUser: 999
          env:
            - name: LOVELY_PLUGINS
              value: argocd-vault-plugin generate -
            - name: PLUGIN_NAME
              value: argocd-lovely-plugin
          volumeMounts:
            # Import the repo-server's plugin binary
            - mountPath: /var/run/argocd
              name: var-files
            - mountPath: /home/argocd/cmp-server/plugins
              name: plugins
              # Starting with v2.4, do NOT mount the same tmp volume as the repo-server container. The filesystem separation helps
              # mitigate path traversal attacks.
            - mountPath: /tmp
              name: lovely-tmp
              # Mount the custom-tools volume, which contains the plugin binary.
            - name: custom-tools
              subPath: argocd-vault-plugin
              mountPath: /usr/local/bin/argocd-vault-plugin
      volumes:
        # A temporary directory for the tool to work in.
        - emptyDir: {}
          name: lovely-tmp
        - name: custom-tools
          emptyDir: {}
