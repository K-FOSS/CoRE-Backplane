apiVersion: argoproj.io/v1alpha1
kind: ConfigManagementPlugin
metadata:
  name: gitpods
spec:
  version: v1.0
  generate:
    command: [sh, /installer render]
  discover:
    fileName: 'gitpod.config.yaml'