apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: core-backplane-network-baremetal-operator
  namespace: argocd
spec:
  generators:
    - list:
        elements:
          - cluster: cntrl-plane
            url: https://10.1.1.44:6443
  template:
    metadata:
      name: '{{cluster}}-baremetal-ops'
    spec:
      project: core
    #   syncPolicy:
    #     automated:
    #       selfHeal: true
    #       prune: true
      source:
        path: config/default
        repoURL: https://github.com/tinkerbell/tink.git
        targetRevision: HEAD

      destination:
        server: '{{url}}'
        namespace: core-prod
