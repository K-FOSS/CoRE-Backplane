apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: core-backplane-development-gitlab-secrets
  namespace: argocd
spec:
  generators:
    - list:
        elements:
          - cluster: k0s-cntrl
            url: https://kubernetes.default.svc
            environment: 'prod'
  template:
    metadata:
      name: '{{cluster}}-development-gitlab-{{environment}}-secrets'
    spec:
      project: core
      syncPolicy:
        automated: {}
      source:
        path: DevelopmentSecrets/SecretsChart
        repoURL: https://gitlab.git.writemy.codes/KJDev/CORE-Backplane.git
        targetRevision: main
        helm:
          parameters:
            - name: secretSuffix
              value: '{{ environment }}'

      destination:
        server: '{{url}}'
        namespace: core-prod
