
apiVersion: v1
kind: Service
metadata:
  name: s3tftp

spec:
  ports:
    - name: udp-tftp
      port: 69
      protocol: UDP
      targetPort: udp-tftp

  selector:
    app: s3tftp

  type: ClusterIP
