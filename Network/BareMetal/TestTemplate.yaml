apiVersion: "tinkerbell.org/v1alpha1"
kind: Template
metadata:
  name: hello_world_workflow
spec:
  data: |
    version: "0.1"
    name: helloworld
    global_timeout: 1800
    tasks:
      - name: "hello world"
        worker: "{{.device_1}}"
        actions:
          - name: "hello_world"
            image: registry.writemy.codes/library/library/hello-world
            timeout: 60
