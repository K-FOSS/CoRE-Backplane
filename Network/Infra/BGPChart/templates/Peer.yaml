{{- $top := . -}}
{{- range $peers := .Values.peers -}}
---
apiVersion: crd.projectcalico.org/v1
kind: BGPPeer
metadata:
  name: {{ $peers.name }}
spec:
  peerIP: {{ $peers.peerIP }}
  asNumber: {{ $peers.asn }}
{{- end }}
