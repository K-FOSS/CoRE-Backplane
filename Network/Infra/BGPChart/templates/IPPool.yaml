apiVersion: crd.projectcalico.org/v1
kind: IPPool
metadata:
  name: default-ipv4-ippool
spec:
  allowedUses:
  - Workload
  - Tunnel
  blockSize: 26
  cidr: {{ .Values.podCIDR }}
  ipipMode: Always
  natOutgoing: false
  nodeSelector: all()
  vxlanMode: Never
