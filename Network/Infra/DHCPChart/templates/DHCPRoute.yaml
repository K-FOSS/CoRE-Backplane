apiVersion: traefik.containo.us/v1alpha1
kind: IngressRouteUDP
metadata:
  name: 'ingress-{{ template "dhcp.fullname" . }}'
spec:
  entryPoints:
    - dhcp
  routes:
    - services:
        - name: '{{ include "dhcp.fullname" . }}-dhcp'
          port: 67
          weight: 10 
