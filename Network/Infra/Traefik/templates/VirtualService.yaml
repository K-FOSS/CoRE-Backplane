apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: traefik
spec:
  hosts:
    - argo.writemy.codes
    - resolvemy.host
    - kjmedia.stream
    - '*.mylogin.space'
    - '*.kjmedia.stream'
  gateways:
    - traefik-gateway
  tls:
    - match:
        - port: 443
          sniHosts:
            - argo.writemy.codes
            - resolvemy.host
            - kjmedia.stream
            - '*.mylogin.space'
            - '*.int.mylogin.space'
            - '*.kjmedia.stream'
      route:
        - destination:
            host: traefik-core
            port:
              number: 443

