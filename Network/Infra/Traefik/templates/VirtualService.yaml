apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: traefik
spec:
  hosts:
    - resolvemy.host
    - controlcenter.house
    - '*.mylogin.space'
    - '*.kjmedia.stream'
    - '*.writemy.codes'
  gateways:
    - traefik-gateway
  tls:
    - match:
        - port: 443
          sniHosts:
            - resolvemy.host
            - controlcenter.house
            - '*.mylogin.space'
            - '*.int.mylogin.space'
            - '*.kjmedia.stream'
            - '*.writemy.codes'
      route:
        - destination:
            host: traefik-core
            port:
              number: 443
          weight: 100

