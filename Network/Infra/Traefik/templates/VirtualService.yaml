apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: traefik
spec:
  hosts:
    - resolvemy.host
    - controlcenter.house
    - '*.mylogin.space'
    - '*.kjmedia.stream'
    - '*.writemy.codes'
    - '*.ipaddr.network'
  gateways:
    - traefik-gateway
  tls:
    - match:
        - port: 443
          sniHosts:
            - resolvemy.host
            - controlcenter.house
            - '*.mylogin.space'
            - '*.int.mylogin.space'
            - '*.kjmedia.stream'
            - '*.writemy.codes'
            - '*.ipaddr.network'
      route:
        - destination:
            host: traefik-core
            port:
              number: 443
          weight: 100

