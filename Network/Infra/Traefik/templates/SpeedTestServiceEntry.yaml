apiVersion: networking.istio.io/v1alpha3
kind: ServiceEntry
metadata:
  name: external-svc-librespeed
spec:
  hosts:
    - librespeed
  location: MESH_EXTERNAL
  ports:
    - number: 80
      name: http
      protocol: HTTP
  resolution: DNS
  endpoints:
    - address: k0s-cntrl-business-testing-librespeed.core-prod.svc.cluster.local
      ports:
        redis: 80
