apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: rdns-udp
spec:
  gateways:
    - rdns-gateway
  hosts:
    - '*'
  tcp:
    - match:
        - port: 53
      route:
        - destination:
            host: dns-rdns-core
            port:
              number: 53
