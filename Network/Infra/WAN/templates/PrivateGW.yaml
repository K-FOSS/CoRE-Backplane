kind: Service
apiVersion: v1
metadata:
  name: private-wan-gw
  labels:
    lan-mode: private
  annotations:
    external-dns.alpha.kubernetes.io/hostname: 'k0s-dc1.resolvemy.host,nat.ipaddr.network,unifi.ipaddr.network,*.git.writemy.codes,accessmyporn.download'
spec:
  type: ExternalName
  externalName: 10.1.1.83
