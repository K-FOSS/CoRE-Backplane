apiVersion: logging.banzaicloud.io/v1beta1
kind: Flow
metadata:
  name: metallb-logging
  namespace: metallb-system
spec:
  match:
    - select:
        labels:
          app.kubernetes.io/name: metallb
  localOutputRefs:
    - loki-output
