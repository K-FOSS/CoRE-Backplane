apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: wirebird
    app.kubernetes.io/instance: signal
  name: wirebird-signal
spec:
  ports:
    - name: signal
      port: 10000
      targetPort: signal
  selector:
    app.kubernetes.io/name: wirebird
    app.kubernetes.io/instance: signal
