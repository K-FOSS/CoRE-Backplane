{{- if .Values.merbridge.enabled }}
# Source: merbridge/templates/serviceaccount.yaml
apiVersion: v1
kind: ServiceAccount
metadata:
  name: merbridge
  namespace: istio-system
  labels:
    app: merbridge

{{- end }}
