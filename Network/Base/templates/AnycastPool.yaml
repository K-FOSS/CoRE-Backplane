{{- if .Values.metallb.enabled }}
apiVersion: metallb.io/v1beta1
kind: IPAddressPool
metadata:
  name: anycast-pool
spec:
  autoAssign: false
  addresses:
    - 10.1.1.0/24
    - 66.165.222.125/32
{{- end }}
