apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: k0s-cntrl-rdns-lab
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: k0s-cntrl-rdns-lab
            port:
              number: 80
