apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

configMapGenerator:
  - name: rdns-pdns-config
    files:
      - pdns.conf

patches:
  - path: ./Sidecar.yaml
    target:
      group: apps
      version: v1
      kind: Deployment
      