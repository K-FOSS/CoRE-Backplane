apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: pomerium-ca
spec:
  isCA: true
  secretName: pomerium-ca
  commonName: pomerium ca
  issuerRef:
    name: pomerium-ca
    kind: Issuer
