apiVersion: gateway.envoyproxy.io/v1alpha1
kind: EnvoyGateway
metadata:
  name: envoy-config

spec:
  provider:
    type: Kubernetes
    kubernetes:
      envoyDeployment:
        replicas: 3

      # envoyService:
      #   annotations:
      #     custom1: svc-annotation1

  extensionApis:
    enableEnvoyPatchPolicy: true