apiVersion: kuadrant.io/v1alpha1
kind: TLSPolicy
metadata:
  name: main-gwtls

spec:
  targetRef:
    group: gateway.networking.k8s.io
    kind: Gateway

    name: main-gw

  issuerRef:
    group: cert-manager.io
    kind: ClusterIssuer
    name: kjdev-cluster 