apiVersion: kuadrant.io/v1beta2
kind: AuthPolicy
metadata:
  name: gw-noauth

spec:
 targetRef:
    group: gateway.networking.k8s.io
    kind: HTTPRoute
    name: auth-route

  rules:
    authentication:
      'authentik':
        jwt:
          issuerUrl: 