apiVersion: authorino.kuadrant.io/v1beta2
kind: AuthConfig
metadata:
  name: defaultallow

spec:
  hosts:
    - '*'

  authentication:
    public:
      anonymous: {}
      credentials:
        authorizationHeader: {}
