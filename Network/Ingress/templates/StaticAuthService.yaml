apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/instance: traefik-core
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/name: s3-proxy
    argocd.argoproj.io/instance: traefik-core
    helm.sh/chart: s3-proxy-2.11.0
  name: s3-static-test

spec:
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: http

  selector:
    app.kubernetes.io/instance: traefik-core
    app.kubernetes.io/name: s3-proxy
  sessionAffinity: None
  type: ClusterIP