---
controllers:
  main:
    containers:
      main:
        image:
          repository: powerdns/pdns-auth-48
          tag: 4.8.4
          pullPolicy: IfNotPresent

service:
  main:
    ports:
      http:
        port: 53