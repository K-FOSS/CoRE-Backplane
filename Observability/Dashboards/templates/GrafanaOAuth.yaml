apiVersion: gateway.envoyproxy.io/v1alpha1
kind: SecurityPolicy
metadata:
  name: core-grafana

spec:
  targetRefs:
    - group: gateway.networking.k8s.io
      kind: HTTPRoute
      name: grafana-core

  oidc:
    provider:
      issuer: https://idp.mylogin.space/application/o/loki-test/
    scopes:
      - openid
      - profile
      - logs
    clientID: loki-test
    forwardAccessToken: true
    clientSecret:
      name: loki-core-sso