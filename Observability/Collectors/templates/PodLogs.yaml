apiVersion: monitoring.grafana.com/v1alpha1
kind: PodLogs
metadata:
  name: core-logs-pod-monitor

spec:
  selector:
    matchLabels:
      logs: loki-myloginspace
  namespaceSelector:
    matchNames:
      - core-prod  # Replace with the namespace(s) you want to monitor
