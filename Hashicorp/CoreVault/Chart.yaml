apiVersion: v2
version: 1.0.0

name: corevault

dependencies:
  #
  # Vault
  #
  - name: vault
    # TODO: Automate version updates
    version: 0.24.1
    repository: https://helm.releases.hashicorp.com
    condition: vault.enabled
