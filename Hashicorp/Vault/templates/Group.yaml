apiVersion: identity.vault.upbound.io/v1alpha1
kind: Group
metadata:
  name: vault-admin
spec:
  forProvider:
    name: admins
    type: external

    metadata:
      version: '2'
      responsibility: 'Manage K/V Secrets'

    policies:
      - admins
