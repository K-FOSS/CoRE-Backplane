{{- if .Values.hub }}
apiVersion: vault.vault.upbound.io/v1alpha1
kind: Mount
metadata:
  name: pki-int-mount
spec:
  providerConfigRef:
    name: vault-provider

  forProvider:
    path: pki_int
    type: pki
    description: "PKI (temporary root) for internal issuance"
{{- end }}