apiVersion: v1
kind: Service
metadata:
  name: helm-controller-service
  namespace: osh-system
  labels:
    app: helm-controller
spec:
  selector:
    app: helm-controller
  ports:
    # tiller probe/metrics port
    - port: 44135