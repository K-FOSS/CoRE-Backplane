---
{{- include "bjw-s.common.loader.init" . }}
{{- define "app-template.hardcodedValues" -}}
{{ if not .Values.global.nameOverride }}
global:
  nameOverride: '{{ .Release.Name }}'
{{ end }}
controllers:
  {{- if .Values.readarr.enabled }}
  readarr:
    strategy: Recreate
    name: 'readarr'
    pod:
      labels:
        mylogin.space/media-volume-affinity: {{ .Values.tenant.name | squote }}
      affinity:
        podAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchLabels:
                    mylogin.space/media-volume-affinity: {{ .Values.tenant.name | squote }}
                topologyKey: 'kubernetes.io/hostname'
      securityContext:
        fsGroup: {{ .Values.user.gid }}
        runAsUser: {{ .Values.user.uid }}
        runAsGroup: {{ .Values.user.gid }}
        fsGroupChangePolicy: 'OnRootMismatch'
    containers:
      readarr:
        image:
          repository: {{ .Values.readarr.image }}
          tag: {{ .Values.readarr.tag }}
          digest: {{ .Values.readarr.digest }}
          pullPolicy: 'IfNotPresent'
        ports:
          - name: 'http'
            containerPort: 8787
  {{- end }}
persistence:
  {{- if .Values.readarr.volumes.config.enabled }}
  readarr-config:
    enabled: true
    type: persistentVolumeClaim
    accessMode: ReadWriteOnce
    size: 2G
    advancedMounts:
      readarr:
        readarr:
          - path: '/config'
  {{- end }}
  {{- if .Values.readarr.volumes.media.enabled }}
  media:
    enabled: true
    type: persistentVolumeClaim
    existingClaim: {{ printf "%s-media" .Values.tenant.name | squote }}
    advancedMounts:
      readarr:
        readarr:
          - path: '/books'
  {{- end }}
  {{- if .Values.readarr.volumes.downloads.enabled }}
  downloads:
    enabled: true
    type: persistentVolumeClaim
    existingClaim: {{ printf "%s-downloads" .Values.tenant.name | squote }}
    advancedMounts:
      readarr:
        readarr:
          - path: '/downloads'
  {{- end }}
service:
  {{- if .Values.readarr.enabled }}
  readarr:
    enabled: true
    controller: 'readarr'
    primary: true
    type: ClusterIP
    ports:
      http:
        enabled: true
        primary: true
        port: 80
        targetPort: 'http'
        protocol: TCP
  {{- end }}
route:
  {{- if .Values.readarr.enabled }}
  readarr:
    enabled: true
    kind: HTTPRoute
    labels:
      wan-mode: 'public'
      lan-mode: 'private'
    hostnames:
      - 'books.mylogin.space'
    parentRefs:
      - group: gateway.networking.k8s.io
        kind: Gateway
        name: {{ .Values.pubgateway.name }}
        sectionName: {{ .Values.pubgateway.sectionName }}
        namespace: {{ .Values.pubgateway.namespace }}
    rules:
      - backendRefs:
          - kind: Service
            port: 80
            identifier: 'readarr'
            weight: 1
  {{- end }}
{{- end -}}
{{- $_ := mergeOverwrite .Values (include "app-template.hardcodedValues" . | fromYaml) -}}
{{ include "bjw-s.common.loader.generate" . }}
