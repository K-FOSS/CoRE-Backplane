---
{{- include "bjw-s.common.loader.init" . }}
{{- define "app-template.hardcodedValues" -}}
{{ if not .Values.global.nameOverride }}
global:
  nameOverride: '{{ .Release.Name }}'
{{ end }}
controllers:
  {{- if .Values.chaptarr.enabled }}
  chaptarr:
    strategy: Recreate
    name: 'chaptarr'
    pod:
      labels:
        mylogin.space/media-volume-affinity: {{ .Values.tenant.name | squote }}
      affinity:
        podAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchLabels:
                    mylogin.space/media-volume-affinity: {{ .Values.tenant.name | squote }}
                topologyKey: 'kubernetes.io/hostname'
      securityContext:
        fsGroup: {{ .Values.user.gid }}
        runAsUser: {{ .Values.user.uid }}
        runAsGroup: {{ .Values.user.gid }}
        fsGroupChangePolicy: 'OnRootMismatch'
    containers:
      chaptarr:
        image:
          repository: {{ .Values.chaptarr.image }}
          tag: {{ .Values.chaptarr.tag }}
          digest: {{ .Values.chaptarr.digest }}
          pullPolicy: 'IfNotPresent'
        ports:
          - name: 'http'
            containerPort: 8789
        env:
          - name: 'PUID'
            value: '{{ .Values.user.uid }}'
          - name: 'PGID'
            value: '{{ .Values.user.gid }}'
          - name: 'UMASK'
            value: '002'
          - name: 'TZ'
            value: '{{ .Values.global.timezone }}'
  {{- end }}
persistence:
  {{- if .Values.chaptarr.volumes.config.enabled }}
  chaptarr-config:
    enabled: true
    type: persistentVolumeClaim
    accessMode: ReadWriteOnce
    size: 2G
    advancedMounts:
      chaptarr:
        chaptarr:
          - path: '/config'
  {{- end }}
  {{- if .Values.chaptarr.volumes.media.enabled }}
  media:
    enabled: true
    type: persistentVolumeClaim
    existingClaim: {{ printf "%s-media" .Values.tenant.name | squote }}
    advancedMounts:
      chaptarr:
        chaptarr:
          - path: '/ebooks'
          - path: '/audiobooks'
  {{- end }}
  {{- if .Values.chaptarr.volumes.downloads.enabled }}
  downloads:
    enabled: true
    type: persistentVolumeClaim
    existingClaim: {{ printf "%s-downloads" .Values.tenant.name | squote }}
    advancedMounts:
      chaptarr:
        chaptarr:
          - path: '/downloads'
  {{- end }}
service:
  {{- if .Values.chaptarr.enabled }}
  chaptarr:
    enabled: true
    controller: 'chaptarr'
    primary: true
    type: ClusterIP
    ports:
      http:
        enabled: true
        primary: true
        port: 80
        targetPort: 'http'
        protocol: TCP
  {{- end }}
route:
  {{- if .Values.chaptarr.enabled }}
  chaptarr:
    enabled: true
    kind: HTTPRoute
    labels:
      wan-mode: 'public'
      lan-mode: 'private'
    hostnames:
      - 'books.mylogin.space'
    parentRefs:
      - group: gateway.networking.k8s.io
        kind: Gateway
        name: {{ .Values.pubgateway.name }}
        sectionName: {{ .Values.pubgateway.sectionName }}
        namespace: {{ .Values.pubgateway.namespace }}
    rules:
      - backendRefs:
          - kind: Service
            port: 80
            identifier: 'chaptarr'
            weight: 1
  {{- end }}
{{- end -}}
{{- $_ := mergeOverwrite .Values (include "app-template.hardcodedValues" . | fromYaml) -}}
{{ include "bjw-s.common.loader.generate" . }}
