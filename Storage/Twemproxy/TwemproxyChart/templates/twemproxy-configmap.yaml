apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "twemproxy.fullname" . }}-twemproxy-config
  labels:
    {{- include "twemproxy.labels" . | nindent 4 }}
data:
  nutcracker.conf: |-
{{ include "twemproxy.nutcrackerConfig" . | indent 4}}
