apiVersion: policy.karmada.io/v1alpha1
kind: PropagationPolicy
metadata:
  name: vitess-sync

spec:
  propagateDeps: true
  resourceSelectors:
    - apiVersion: apps/v1
      kind: Deployment
      name: vitess-operator

  placement:
    clusterAffinity:
      clusterNames:
        - k0s-dc1