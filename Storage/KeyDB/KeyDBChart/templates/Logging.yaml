apiVersion: logging.banzaicloud.io/v1beta1
kind: Flow
metadata:
  name: '{{ include "keydb.fullname" . }}-logflow'
spec:
  match:
    - select:
        labels:
          app.kubernetes.io/name: keydb
  localOutputRefs:
    - loki-output
