apiVersion: networking.istio.io/v1beta1
kind: ServiceEntry
metadata:
  name: minio-main
spec:
  hosts:
    - minio.core-prod.svc.k0s.resolvemy.host
  location: MESH_INTERNAL
  ports:
    - number: 443
      name: https
      protocol: TLS
  resolution: DNS
