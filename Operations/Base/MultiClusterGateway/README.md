# Multicluster Gateway base deployment

This directory currently contains an empty `Chart.yaml` and no local templates
or Kustomization. No direct ApplicationSet owner was found.

It is an unfinished or reserved deployment boundary and produces no known
resources through the checked-in files. Document the intended gateway
controller, resource model, ownership and migration plan before activation.
