env: prod

debug: false

crossplane:
  functionsRef:
    gotemplate:
      name: function-go-templating

s3:
  terraform:
    provider: s3-prod
  
  crossplane:
    provider: s3-prod

ldap:
  server: ldap.mylogin.space