apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: mongodb-operator-prod
  namespace: operators
spec:
  channel: stable
  name: percona-server-mongodb-operator
  source: operatorhubio-catalog
  sourceNamespace: operator-lifecycle-manager
