apiVersion: kci.rocks/v1beta1
kind: DbInstance
metadata:
  name: core-mysql
spec:
  adminSecretRef:
    Name: mysql-admin-secret
    Namespace: core-prod

  engine: mysql
  generic:
    host: mysql.service.dc1.kjdev
    port: 3306
