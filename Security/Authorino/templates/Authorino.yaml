apiVersion: operator.authorino.kuadrant.io/v1beta1
kind: Authorino
metadata:
  name: authorino
spec:

  logLevel: 'DEBUG'
  listener:
    tls:
      enabled: false

  oidcServer:
    tls:
      enabled: false