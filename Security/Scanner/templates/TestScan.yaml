apiVersion: execution.securecodebox.io/v1
kind: Scan
metadata:
  name: 'nmap-scanme.nmap.org'

  annotations:
    defectdojo.securecodebox.io/product-type-name: "Test"
spec:
  scanType: nmap
  parameters:
    - scanme.nmap.org