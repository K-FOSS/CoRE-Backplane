apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: networkglass-default-certificates
spec:
  secretName: networkglass-default-certificates
  commonName: network.glass
  dnsNames:
    - network.glass
    - '*.network.glass'
  issuerRef:
    name: kjdev-cluster
    kind: ClusterIssuer
