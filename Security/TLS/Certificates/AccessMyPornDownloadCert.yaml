apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: accessmyporndownload-default-certificates
spec:
  secretName: accessmyporndownload-default-certificates
  commonName: accessmyporn.download
  dnsNames:
    - accessmyporn.download
    - '*.accessmyporn.download'
  issuerRef:
    name: kjdev-cluster
    kind: ClusterIssuer
