apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: syncmydate-default-certificates
spec:
  secretName: syncmydate-default-certificates
  commonName: syncmy.date
  dnsNames:
    - syncmy.date
    - '*.syncmy.date'
  issuerRef:
    name: kjdev-cluster
    kind: ClusterIssuer
