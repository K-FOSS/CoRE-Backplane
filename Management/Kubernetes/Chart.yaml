apiVersion: v2
version: 1.0.0

name: core-management-kubernetes

dependencies:
  #
  # Headlamp
  #
  - name: headlamp
    version: '>= 0.28.0'
    repository: https://kubernetes-sigs.github.io/headlamp/
    condition: headlamp.enabled
