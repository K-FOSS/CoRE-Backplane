apiVersion: external-secrets.io/v1alpha1
kind: ExternalSecret
metadata:
  name: development-gitlab-shell-{{ .Values.secretSuffix }}-sync
spec:
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: gitlab-shell-{{ .Values.secretSuffix }}
  data:
    - secretKey: shared_secret
      remoteRef:
        key: GitLab
        property: ShellSecret
